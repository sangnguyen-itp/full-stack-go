package constants

// ENVIRONMENT
const (
	DEV        = "dev"
	TEST       = "test"
	PRODUCTION = "production"
)

// DATABASE
const (
	POSTGRES = "postgres"
	MYSQL    = "mysql"
	MSSQL    = "mssql"
)

// CACHING
const (
	REDIS = "redis"
	MEMCACHED = "memcached"
)